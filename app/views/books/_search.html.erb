<%= form_with url: search_contents_path, local: true do |f| %>
  <%= f.text_field :search_word %>
  <%= f.select :search_model, [["User", 0], ["Book", 1]] %>
  <%= f.select :search_level, [["完全一致", 0], ["前方一致", 1], ["後方一致", 2], ["部分一致", 3]] %>
  <%= f.submit "検索" %>
<% end %>